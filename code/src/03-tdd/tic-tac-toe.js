// This function shall return who wins the game of tic tac toe.
// Each row shall have three fields with either 'X', 'O', or ' ' in it.
// Any other lengths or contents are invalid.
// Return values: 'X wins', 'O wins', 'Nobody won yet', 'Invalid input' (for too long rows, invalid contents, etc.)

function whoWins(row1, row2, row3) {
	return undefined;
}

module.exports = { whoWins }